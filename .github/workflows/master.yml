name: CI

on:
  push:
    branches: [ master ]
    
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@v0.1.7
      with:
        host: 103.150.87.242
        username: radenfebri
        password: Febri2303
        port: 22
        script: |
          sudo cd /home/radenfebri/website
          sudo git config --global --add safe.directory /home/radenfebri/website
          sudo git pull origin master
          
